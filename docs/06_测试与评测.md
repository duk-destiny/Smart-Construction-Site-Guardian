# 06 · 测试与评测

> 返回 [README](../README.md) · 上一节 [05 API 接口](05_API接口.md) · 下一节 [07 部署与生产安全](07_部署与生产安全.md)

## 测试体系

| 层 | 命令 | 覆盖 |
| --- | --- | --- |
| 全量单元/集成 | `python scripts/run_tests.py` | tests/ 下 50+ 测试文件（编排器/融合/规范/复核/处置、RAG、告警生命周期、工单闭环、权限、反馈闭环、模型注册、认知层降级矩阵/安全边界/挂起恢复/剧本回溯、API 34 例 httpx AsyncClient 全端点走查等） |
| UI 端到端（Streamlit） | `python scripts/e2e_apptest.py --group safe\|agents\|realtime\|diag\|nav` | AppTest 分进程，无需浏览器 |
| 前端组件冒烟 | `cd frontend && npm run test` | Vitest 组件冒烟 |
| 浏览器关键流程冒烟 | `python scripts/api_browser_smoke.py` | Playwright + chromium：登录→强制改密→文字上报→查结果→提交整改→验收销项；服务端以临时库启动不污染开发库 |
| 浏览器真机验收 | `python scripts/browser_acceptance.py` | v0.2~v0.5 演示故事线全流程截图 |
| 静态检查 | `ruff check .` | Lint（含 api/ 目录） |

v1.0 发布基线：pytest 288 passed + AppTest 4 passed；API 34 例 / Vitest 5 例 / Playwright 双链路全部 PASS；ruff 全绿；CI（Ubuntu/Py3.13）首次真正执行并全绿。v2.1 新增 22 例（降级矩阵/安全边界/挂起恢复/剧本回溯）；v2.2 新增对话窗口/会话管理/附件注入/工单问询等用例，全量 387 passed。UI 专项：Playwright e2e 六阶段（登录/双窗口/工单/实时历史/管理端/研判页）+ 浏览器全链路冒烟（登录→改密→工具卡建单→查结果→派发→整改→验收）+ Vitest 组件冒烟；主题切换（暗色/亮色、主色预设、localStorage 持久化）与上下文记忆注入有后端用例钉死。

## 模型评测基线

评测分两个口径，互补参照：

- **训练验证集 mAP**（整体）：复训时从 `results.csv` best epoch 写入 `model_registry`，管理端「模型版本注册」区展示，口径偏乐观，用于版本粗筛；
- **独立测试集 P/R/F1**（逐类）：`evaluate_models.py` 在测试集上逐图推理，写入 `data/eval/model_eval.json`，管理端「模型评估摘要」表展示，暴露单类弱点、是优化方向的依据。
  - 正式口径为**线上一致阈值**（`role=configured`，与 `config.yaml` 场景 `conf_thres` 完全一致的单一阈值）——部署判定点是什么就报什么；
  - 阈值扫描结果标注 `role=sweep`，属于同一测试集上的「同集选优」，数字系统性偏乐观，仅用于观察阈值敏感性，不作为效果承诺。

```bash
# 评测所有已注册版本（默认）
python scripts/evaluate_models.py --thresholds 0.25 0.30 0.35 0.45
# 只评测指定版本
python scripts/evaluate_models.py --version v3
```

模型路径从 `model_registry` 读取（不再硬编码），按「场景+版本」聚合 merge 写入（不覆盖已有版本）。新增模型后跑一次本脚本即可补入独立测试集评测，无需改代码。

实测基线（最佳阈值，独立测试集）：

**火情**（55 张测试图）：

| 版本 | 类别 | 最佳阈值 | Precision | Recall | F1 |
| --- | --- | ---: | ---: | ---: | ---: |
| v2（当前活跃） | 烟雾 | 0.35 | 0.91 | 0.91 | 0.91 |
| v2（当前活跃） | 火花 | 0.35 | 0.82 | 0.77 | 0.79 |

**PPE**（90 张测试图，节选）：

| 版本 | 类别 | 最佳阈值 | Precision | Recall | F1 |
| --- | --- | ---: | ---: | ---: | ---: |
| v3（当前活跃） | 佩戴安全帽 | 0.25 | 0.87 | 0.87 | 0.87 |
| v3（当前活跃） | 未戴安全帽 | 0.25 | 0.88 | 0.62 | 0.73 |
| v3（当前活跃） | 未穿反光衣 | 0.25 | 0.89 | 0.66 | 0.75 |
| v3（当前活跃） | 穿着反光衣 | 0.25 | 0.91 | 0.78 | 0.84 |
| v3（当前活跃） | 人员 | 0.25 | 0.90 | 0.47 | 0.62 |
| v4（备用） | 佩戴安全帽 | 0.25 | 0.87 | 0.84 | 0.86 |
| v4（备用） | 未戴安全帽 | 0.25 | 0.89 | 0.71 | 0.79 |
| v4（备用） | 人员 | 0.25 | 0.91 | 0.38 | 0.54 |

结论：火情 v2 在 0.30–0.35 阈值 F1 最佳（已在 config 落地）；PPE v3 逐类全面优于 v2，已切换为活跃版本；v4 相比 v3 整体 mAP 略升（0.695→0.704）、`no_helmet` 召回 0.62→0.71 提升明显，但 `person` 召回 0.47→0.38 略降且仍偏低，是后续定位损失加权与数据补采的优先项。

### INT8 量化评测（线上一致阈值口径）

三类模型的 INT8 动态量化副本（`scripts/quantize_models.py`，44.7→11.5MB，3.9x）注册后由 `evaluate_models.py` 自动纳入同口径评测（`role=configured`，与上表 FP32 完全同集同阈值，数据见 `data/eval/model_eval.json` 的 `*-int8` 版本）：

| 模型 | 类别 | FP32 F1 | INT8 F1 | Δ |
| --- | --- | ---: | ---: | ---: |
| fire v2 | 烟雾 | 0.909 | 0.909 | 0 |
| fire v2 | 火花 | 0.794 | **0.725** | -6.9% |
| ppe v3 | 佩戴安全帽 | 0.870 | 0.864 | -0.6% |
| ppe v3 | 未戴安全帽 | 0.732 | **0.762** | +3.0% |
| ppe v3 | 未穿反光衣 | 0.755 | **0.706** | -4.9% |
| ppe v3 | 人员 | 0.620 | 0.586 | -3.4% |
| ppe v3 | 穿着反光衣 | 0.842 | 0.837 | -0.5% |
| ppe v4 | 佩戴安全帽 | 0.856 | 0.851 | -0.5% |
| ppe v4 | 未戴安全帽 | 0.791 | 0.791 | 0 |
| ppe v4 | 未穿反光衣 | 0.759 | 0.765 | +0.6% |
| ppe v4 | 人员 | 0.539 | 0.515 | -2.4% |
| ppe v4 | 穿着反光衣 | 0.835 | 0.831 | -0.4% |

**结论：INT8 全部维持 active=0 不顶替线上。** 多数类损失 ≤1%，但火花 -6.9%、未穿反光衣 -4.9% 的类级损失超出预估；量化副本定位为**体积敏感的边缘部署选项**，精度敏感的线上继续 FP32，达标类别按场景复评后先行切换（见 09 优化计划）。

## 端到端评测指标

`scripts/eval_metrics.py` 实测（CPU，单帧/单次查询）：

| 维度 | 指标 | 值 | 说明 |
| --- | --- | ---: | --- |
| 检测延迟 | 火情头单帧 | ~436 ms | YOLOv8s ONNX，CPU |
| 检测延迟 | PPE 头单帧 | ~422 ms | YOLOv8 ONNX，CPU |
| 检测延迟 | 双头并行 | ~211 ms | ThreadPoolExecutor，取最大而非求和 |
| 检测延迟 | 双头串行 | ~858 ms | 对比基线，并行省约 75% |
| RAG | 知识库条目 | 22 块 | 动火作业规范切分 |
| RAG | 召回率@5 | 1.0 | chunk 派生查询（上界） |
| RAG | 平均查询延迟 | ~78 ms | BGE encode + Chroma cosine |
| RAG | 领域查询 top1 | 0.54–0.84 | 「监火人」0.84 / 「可燃气体」0.54 |

检测 mAP 见管理端「模型版本注册」（火情 v2 mAP50=0.898、PPE v3 mAP50=0.695 / v4 mAP50=0.704，训练验证集）。实时链路双头并行后单帧 ~0.21s，满足「采样节奏 ≠ 告警 SLA」下的连续监测；RAG 查询百毫秒级，不阻塞告警（条款异步回填）。
